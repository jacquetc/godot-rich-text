name: Build GDExtension

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── Lint & test ──────────────────────────────────────────────
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2

      - name: Strip path dependencies
        uses: ./.github/actions/strip-path-deps

      - name: Format check
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings -A deprecated

      - name: Test
        run: cargo test

  # ── Build per platform ───────────────────────────────────────
  build:
    needs: check
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            output_name: libgodot_rich_text.so
            release_name: libgodot_rich_text.linux.release.x86_64.so
            debug_name: libgodot_rich_text.linux.debug.x86_64.so
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            output_name: godot_rich_text.dll
            release_name: godot_rich_text.windows.release.x86_64.dll
            debug_name: godot_rich_text.windows.debug.x86_64.dll
          - os: macos-latest
            target: x86_64-apple-darwin
            output_name: libgodot_rich_text.dylib
            release_name: libgodot_rich_text.macos.x86_64.dylib
            debug_name: libgodot_rich_text.macos.debug.x86_64.dylib
          - os: macos-latest
            target: aarch64-apple-darwin
            output_name: libgodot_rich_text.dylib
            release_name: libgodot_rich_text.macos.aarch64.dylib
            debug_name: libgodot_rich_text.macos.debug.aarch64.dylib

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: build-${{ matrix.target }}

      - name: Strip path dependencies
        uses: ./.github/actions/strip-path-deps

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build debug
        run: cargo build --target ${{ matrix.target }}

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.release_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.output_name }}

      - name: Upload debug artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.debug_name }}
          path: target/${{ matrix.target }}/debug/${{ matrix.output_name }}

  # ── macOS universal binary ───────────────────────────────────
  macos-universal:
    needs: build
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - name: Download release x86_64
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.x86_64.dylib
          path: release/x86_64

      - name: Download release aarch64
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.aarch64.dylib
          path: release/aarch64

      - name: Create release universal binary
        run: |
          lipo -create \
            release/x86_64/libgodot_rich_text.dylib \
            release/aarch64/libgodot_rich_text.dylib \
            -output libgodot_rich_text.macos.release.universal.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: libgodot_rich_text.macos.release.universal.dylib
          path: libgodot_rich_text.macos.release.universal.dylib

      - name: Download debug x86_64
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.debug.x86_64.dylib
          path: debug/x86_64

      - name: Download debug aarch64
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.debug.aarch64.dylib
          path: debug/aarch64

      - name: Create debug universal binary
        run: |
          lipo -create \
            debug/x86_64/libgodot_rich_text.dylib \
            debug/aarch64/libgodot_rich_text.dylib \
            -output libgodot_rich_text.macos.debug.universal.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: libgodot_rich_text.macos.debug.universal.dylib
          path: libgodot_rich_text.macos.debug.universal.dylib

  # ── Package & release ───────────────────────────────────────
  release:
    needs: [build, macos-universal]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Strip path dependencies
        uses: ./.github/actions/strip-path-deps

      - name: Verify version matches tag
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"
          CARGO_VERSION=$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "godot-rich-text") | .version')
          if [[ "$TAG_VERSION" != "$CARGO_VERSION" ]]; then
            echo "::error::Tag version ($TAG_VERSION) does not match Cargo.toml version ($CARGO_VERSION)"
            exit 1
          fi
          echo "Version validated: $CARGO_VERSION"

      - name: Copy LICENSE and README to addon folder
        run: |
          cp LICENSE godot/addons/godot_rich_text/
          cp README.md godot/addons/godot_rich_text/

      - name: Create addon structure
        run: |
          mkdir -p pkg/addons/godot_rich_text/bin
          mkdir -p pkg/addons/godot_rich_text/icons

          cp godot/addons/godot_rich_text/godot_rich_text.gdextension pkg/addons/godot_rich_text/
          cp godot/addons/godot_rich_text/icons/*.svg pkg/addons/godot_rich_text/icons/ 2>/dev/null || true
          cp godot/addons/godot_rich_text/LICENSE pkg/addons/godot_rich_text/
          cp godot/addons/godot_rich_text/README.md pkg/addons/godot_rich_text/

      - name: Download Linux release binary
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.linux.release.x86_64.so
          path: tmp/linux
      - run: cp tmp/linux/libgodot_rich_text.so pkg/addons/godot_rich_text/bin/libgodot_rich_text.linux.release.x86_64.so

      - name: Download Linux debug binary
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.linux.debug.x86_64.so
          path: tmp/linux-debug
      - run: cp tmp/linux-debug/libgodot_rich_text.so pkg/addons/godot_rich_text/bin/libgodot_rich_text.linux.debug.x86_64.so

      - name: Download Windows release binary
        uses: actions/download-artifact@v4
        with:
          name: godot_rich_text.windows.release.x86_64.dll
          path: tmp/windows
      - run: cp tmp/windows/godot_rich_text.dll pkg/addons/godot_rich_text/bin/godot_rich_text.windows.release.x86_64.dll

      - name: Download Windows debug binary
        uses: actions/download-artifact@v4
        with:
          name: godot_rich_text.windows.debug.x86_64.dll
          path: tmp/windows-debug
      - run: cp tmp/windows-debug/godot_rich_text.dll pkg/addons/godot_rich_text/bin/godot_rich_text.windows.debug.x86_64.dll

      - name: Download macOS release universal binary
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.release.universal.dylib
          path: tmp/macos
      - run: cp tmp/macos/libgodot_rich_text.macos.release.universal.dylib pkg/addons/godot_rich_text/bin/

      - name: Download macOS debug universal binary
        uses: actions/download-artifact@v4
        with:
          name: libgodot_rich_text.macos.debug.universal.dylib
          path: tmp/macos-debug
      - run: cp tmp/macos-debug/libgodot_rich_text.macos.debug.universal.dylib pkg/addons/godot_rich_text/bin/

      - name: List release contents
        run: ls -lhR pkg/

      - name: Create release archive
        run: |
          cd pkg
          zip -r ../godot-rich-text-${{ github.ref_name }}.zip addons/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: godot-rich-text-${{ github.ref_name }}.zip
          generate_release_notes: true
